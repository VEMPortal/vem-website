# Files that live in the repo (for version history + rebuilding) but must
# NOT be served on the public Vercel deployment. Only the generated static
# output (html, css, js, assets, blog/, insights/) should be published.

# Build tooling & content sources (markdown drafts must never be public)
build_content.py
templates/
content/
*.md
robots.production.txt

# Project tooling / docs
.claude/
.gitignore
.vercelignore

# Stray files — keep in repo, but must NOT be served publicly
hero/
lighthouse-report.report.html
