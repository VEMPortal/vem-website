# Audit / working artifacts (kept locally, not part of the site)
lighthouse-report.report.html
lighthouse-report.report.json
*.log

# Raw multi-MB image generations (we commit only the optimized web versions)
assets/img/Abstract_*.jpeg
assets/img/Elegant_*.jpeg

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
graphify-out/

# Compliance scripts must NEVER live in this public repo — they hold reviewer
# names and internal compliance analysis, and this repo deploys to a public
# website. They live locally in the OneDrive compliance folder (_scripts/).
scripts/fill_marketing_records.py
scripts/generate_marketing_record.py
scripts/MARKETING_RECORD_WORKFLOW.md
scripts/build_combined_records.py
