# Moved — see the global `blog-post` skill

The canonical pre-publish checklist now lives at:

```
~/.claude/skills/blog-post/references/pre-publish-checklist.md
```

It moved so that posts drafted in Cowork (or any workspace that isn't this
repo) get reviewed against the exact same list as posts drafted here, and so
the list only has to be maintained in one place.

**Edit the global copy, not this file.** This stub exists only to stop
someone updating a version that nothing reads.

The site-repo-specific items — `build_content.py --validate`, hero variants,
`CACHE`, preview links, publish, Marketing Record — are in the "Before It
Actually Goes Live" section of that same global checklist, and in this
skill's own `SKILL.md`.
